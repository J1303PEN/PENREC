import Link from "next/link";import { requireAdmin } from "@/lib/auth";import { getManagedArtists } from "@/lib/catalogue-manager";import { ReleaseForm } from "@/components/catalogue17/release-form";import { saveRelease } from "@/app/admin/catalogue/manager-actions";
export default async function NewRelease(){await requireAdmin();const artists=await getManagedArtists();return <main className="admin-page shell inside catalogue-editor"><header className="account-hero"><div><p className="eyebrow">PENREC17 / Release Manager</p><h1>New release</h1><p>Create an artist first if the list is empty.</p></div><Link className="button button--outline" href="/admin/catalogue">Back</Link></header><ReleaseForm artists={artists} action={saveRelease}/></main>}
