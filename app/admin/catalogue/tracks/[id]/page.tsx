import Link from "next/link";import { notFound } from "next/navigation";import { requireAdmin } from "@/lib/auth";import { getManagedTrack } from "@/lib/catalogue-manager";import { TrackForm } from "@/components/catalogue17/track-form";import { saveTrack } from "@/app/admin/catalogue/manager-actions";
export default async function EditTrack({params}:{params:Promise<{id:string}>}){await requireAdmin();const {id}=await params;const track=await getManagedTrack(id);if(!track)notFound();return <main className="admin-page shell inside catalogue-editor"><header className="account-hero"><div><p className="eyebrow">PENREC17 / Track Manager</p><h1>{track.title}</h1><p>ISRC is optional and may be assigned later.</p></div><Link className="button button--outline" href={`/admin/catalogue/releases/${track.release_id}`}>Back to release</Link></header><TrackForm track={track} releaseId={track.release_id} action={saveTrack}/></main>}
