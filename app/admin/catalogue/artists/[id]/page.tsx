import Link from "next/link";import { notFound } from "next/navigation";import { requireAdmin } from "@/lib/auth";import { getManagedArtist } from "@/lib/catalogue-manager";import { ArtistForm } from "@/components/catalogue17/artist-form";import { saveArtist } from "@/app/admin/catalogue/manager-actions";
export default async function EditArtist({params}:{params:Promise<{id:string}>}){await requireAdmin();const {id}=await params;const artist=await getManagedArtist(id);if(!artist)notFound();return <main className="studio-new catalogue-editor"><header className="studio-new__hero"><div><p className="studio-new__eyebrow">PENREC Studio · Catalogue</p><h1>{artist.name}</h1><p>Edit the artist record used across PENREC. Status: {artist.status}.</p></div><nav className="catalogue17-actions"><Link className="button button--outline" href="/admin/catalogue">Back to catalogue</Link></nav></header><ArtistForm artist={artist} action={saveArtist}/></main>}
