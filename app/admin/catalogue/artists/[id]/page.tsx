import Link from "next/link";import { notFound } from "next/navigation";import { requireAdmin } from "@/lib/auth";import { getManagedArtist } from "@/lib/catalogue-manager";import { ArtistForm } from "@/components/catalogue17/artist-form";import { saveArtist } from "@/app/admin/catalogue/manager-actions";
export default async function EditArtist({params}:{params:Promise<{id:string}>}){await requireAdmin();const {id}=await params;const artist=await getManagedArtist(id);if(!artist)notFound();return <main className="admin-page shell inside catalogue-editor"><header className="account-hero"><div><p className="eyebrow">PENREC17 / Artist Manager</p><h1>{artist.name}</h1><p>Artist status: {artist.status}</p></div><Link className="button button--outline" href="/admin/catalogue">Back</Link></header><ArtistForm artist={artist} action={saveArtist}/></main>}
