import Image from "next/image"; import Link from "next/link"; import { artists } from "@/data/catalog";
export default function ArtistsPage(){return <main id="content" className="inside listing-page"><div className="shell"><p className="eyebrow">PENREC roster</p><h1>Artists</h1><p className="listing-page__intro">Distinctive voices. One independent creative home.</p><div className="roster-list">{artists.map((a,i)=><Link href={`/artists/${a.slug}`} className="roster-row" key={a.slug}><span>{String(i+1).padStart(2,"0")}</span><Image src={a.profile} width={180} height={180} alt="" style={{objectPosition:a.profilePosition ?? "50% 50%"}}/><div><h2>{a.name}</h2><p>{a.descriptor}</p></div><b>Discover ↗</b></Link>)}</div></div></main>}
