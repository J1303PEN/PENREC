import Image from "next/image"; import Link from "next/link"; import { notFound } from "next/navigation"; import { artists } from "@/data/catalog"; import { getResolvedArtist } from "@/lib/catalogue-live"; import { AudioPlayer } from "@/components/audio-player"; import { TrackList } from "@/components/track-list";
export function generateStaticParams(){return artists.map(({slug})=>({slug}))}
export default async function ReleasePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=await getResolvedArtist(slug);if(!a)notFound();return <main id="content" className="release-page"><section className="release-hero"><div className="shell release-hero__grid"><Image src={a.cover} alt={`${a.album} cover`} width={760} height={760} priority/><div><p className="eyebrow">PENREC release · {a.catalogue}</p><h1>{a.album}</h1><Link href={`/artists/${a.slug}`} className="release-hero__artist">{a.name} ↗</Link><p className="release-hero__meta">Album · {a.year} · {a.tracks.length} tracks</p><AudioPlayer artist={a} compact/><div className="listening-status"><span>Available in the PENREC player</span><strong>{a.tracks.filter((track) => track.audio).length} complete track ready to play</strong></div></div></div></section><section className="section shell track-section"><header><p className="eyebrow">Album sequence</p><h2>Track listing</h2></header><TrackList artist={a}/><div className="future-module"><span>About this release</span><p>{a.bio[1] ?? a.bio[0]}</p></div></section></main>}
